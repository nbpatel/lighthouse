# CPU tiling.
Pipeline:
  - schedule: "tile_and_fuse.py[gen=assign_and_propagate_tile_sizes]{
      strategy=$strategy
      propagate=$propagate
    }"
  - schedule: "tile_and_fuse.py[gen=assign_elementwise_tile_sizes]{
      strategy=$strategy
      propagate=$propagate
    }"
  - schedule: "tile_and_fuse.py[gen=tile_annotated]{
      use_forall=$use_forall
    }"
